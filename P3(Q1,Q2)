##code:1:Threading and Single Thread Control Flow
import threading
import time

# Student Details
print("Name     : Trisha")
print("Roll No. : S086")

# Function to simulate a task
def task(name):
    print(f"{name} started")
    time.sleep(2)
    print(f"{name} finished")


# Sequential Execution
print("\n--- Sequential Execution ---")

start_time = time.time()

task("Task 1")
task("Task 2")

end_time = time.time()

print("Sequential Execution Time:",
      round(end_time - start_time, 2), "seconds")


#Threaded Execution 
print("\n--- Threaded Execution ---")

start_time = time.time()

t1 = threading.Thread(target=task, args=("Task 1",))
t2 = threading.Thread(target=task, args=("Task 2",))

t1.start()
t2.start()

t1.join()
t2.join()

end_time = time.time()

print("Threaded Execution Time:",
      round(end_time - start_time, 2), "seconds")

#code:2Apply your understanding of threading by running four tasks both sequentially
#and using threads. Measure and compare the execution time.
#• Modify the code to include 4 tasks instead of 2.
#• Each task should:
#- Print when it starts and ends
#- Simulate a delay using time.sleep(2)
#• First, run all 4 tasks sequentially (one after the other).
#• Then, run all 4 tasks using threads (all at the same time).
#• Compare how long it takes to complete all tasks in both approaches. Which one is
#more efficient and why?
import threading
import time

# Student Details
print("Name     : Trisha")
print("Roll No. : S086")

# Function to simulate a task
def task(name):
    print(f"{name} started")
    time.sleep(2)
    print(f"{name} finished")


#Sequential Execution
print("\n--- Sequential Execution ---")

start_time = time.time()

task("Task 1")
task("Task 2")
task("Task 3")
task("Task 4")

end_time = time.time()

print("Sequential Execution Time:",
      round(end_time - start_time, 2), "seconds")


#Threaded Execution
print("\n--- Threaded Execution ---")

start_time = time.time()

t1 = threading.Thread(target=task, args=("Task 1",))
t2 = threading.Thread(target=task, args=("Task 2",))
t3 = threading.Thread(target=task, args=("Task 3",))
t4 = threading.Thread(target=task, args=("Task 4",))

t1.start()
t2.start()
t3.start()
t4.start()

t1.join()
t2.join()
t3.join()
t4.join()

end_time = time.time()

print("Threaded Execution Time:",
      round(end_time - start_time, 2), "seconds")
